@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="card shadow-sm">
    <div class="card-body">
        <h2 class="h5 mb-2">Acceso denegado</h2>
        <p class="text-muted mb-3">No tienes permisos para acceder a esta sección.</p>
        <a asp-controller="Auth" asp-action="Login" class="btn btn-primary">Volver al login</a>
    </div>
</div>

